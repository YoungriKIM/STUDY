# machine 폴더 안에 있는 것들을 불러오자

import machine.car 
import machine.tv

machine.car.drive()
machine.tv.watch()

# ================================
# 운전하다2
# 시청하다2

# 여기서도 machine 을 패키지라고 하기로 한다. 역시 가장 마지막인 car, tv을 모듈이라고 한다.
# 이 전 버전에는 패키지 폴더 안에 <__int__.py> 를 넣어야만 했다.